cases:
  search:
    steps:
      - ${message-page.to-search}
      - ${search(${keyword})}
    asserts:
      - type: contains # 类型需要有个对照enum
        expected: ${expected}
        actual: ${get-texts}
      # - type: xxx
