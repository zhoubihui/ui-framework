config:
  selectorUrl: selector/

# PO中的方法
methods:
  to-search:
    steps:
      - selector: ${message-selector.to-search}
        action: click
    # next: search-page # 是否有必要,下一个页面
  to-address:
    steps:
      - selector: ${message-selector.to-address}
        action: click