methods:
  search:
    params: # 表示这个po方法需要的参数
      - keyword
    steps:
      - element:
          selector: ${search}
          action: input
          data:
            - ${keyword}

  get-texts:
    steps:
      - element:
          selector: ${search-result}
          action: get
